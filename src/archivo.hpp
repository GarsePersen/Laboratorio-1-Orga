#ifndef _H_ARCHIVO_
#define _H_ARCHIVO_

#include <string>

using namespace std;

class Arhcivo{
    public:
        Arhcivo();
	void leerArchivo(string nombreArchivo);
	void escribirArchivo();
};

#endif
